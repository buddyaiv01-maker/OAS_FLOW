// Stub port of legacy_UI/index.html's #credentialsView — node picker + saved credential list,
// including the shared/private toggle from Phase 5.
export default function CredentialsView() {
  return (
    <div className="credentials-view">
      <div className="dashboard-head">
        <div>
          <h2>Credentials</h2>
          <p>Every connection your nodes can use, in one place.</p>
        </div>
      </div>
    </div>
  );
}

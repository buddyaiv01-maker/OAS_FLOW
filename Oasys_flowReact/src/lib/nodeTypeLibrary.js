// Port target for the `nodeTypeLibrary` object in legacy_UI/app.js (~line 200 on): every node
// type's badge/label/cat/color/icon/connection/params/outputFields/execute. This is pure data +
// functions, no DOM access, so it should port over almost unchanged.
export const nodeTypeLibrary = {};
